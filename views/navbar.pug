nav(class='navbar navbar-theme')
    div(class='container')
        div(class='navbar-header')
            a(href='/', class='navbar-brand') Technical University Library
        div(class='collapse navbar-collapse', id='bs-example-navbar-collapse-1')
            ul(class='nav navbar-nav navbar-right')
                li
                    if isAuth
                        a(href="/logout") Logout
                    else
                        a(href="/login") Login
            if isAuth
                a(href="/admin" class='btn btn-danger navbar-btn') Admin panel
            form(method="GET", action="/search", class="navbar-form navbar-left")
                div(class="form-group")
                    input(class="form-control", placeholder="Search..." name="q")
                button(type="submit", class="btn btn-default") Search